from matplotlib import pyplot as plt
import string
from random import random, randrange


def generate(dictionary: str, length: int) -> str:
    return [dictionary[random() for i in range(length)]



if __name__ == '__main__':
    dictionary = list(string.ascii_letters)

